<h2>All categories</h2>

<% categories = Category.find :all, :limit => 6 %>
<p>
  <% categories.each do |item| %>
  <% @category = item %>
  <%= link_to item.name, cat_url(item.id) %><br/>
  <% end %>
</p>